import qrcodeIcon from '../imgs/qrcode.svg'
import sendIcon from '../imgs/send.svg'

export default function AssetScreen({ asset }){
    return(
        <div style={{display:'flex', flexDirection:'row', justifyContent:'center', width:'80vw'}}>
            <div style={{border:'black 1px solid', borderRadius:'5px', flexDirection:'column', justifyContent:'center', width:'80vw'}}>
            <div style={{display:'flex', flexDirection:'row', justifyContent:'center', margin:'3vw'}}>
            </div>
            </div>
        </div>
    );
}